--- Application Configuration ---

NODE_ENV=development
PORT=3000

--- Security ---

In a real app, this should be a long, random, secret string.

JWT_SECRET=super-secret-mini-insta-key
JWT_EXPIRES_IN=1h

--- Database (Mocked in db.js, but included for structure) ---

DB_HOST=localhost
DB_PORT=5432
DB_USER=insta_user
DB_PASSWORD=secret_password
DB_NAME=mini_insta